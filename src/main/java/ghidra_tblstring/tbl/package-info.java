/* (C) Arnaud 'red' Rouyer 2026 */
/**
 * Pure {@code .tbl} parsing and decoding domain.
 *
 * <p>This package intentionally has no Ghidra dependencies. It owns the portable model for table
 * entries, parsed tables, and byte-to-text decoding; the Ghidra package is responsible for storage,
 * UI, and datatype integration.
 */
package ghidra_tblstring.tbl;
